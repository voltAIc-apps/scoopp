apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scoopp-data
  namespace: scoopp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
